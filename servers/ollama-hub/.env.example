# Base URL for the Ollama runtime
OLLAMA_HOST=http://localhost:11434

# Default model to use when none is provided in /chat requests
AGENT_MODEL_OLLAMA=llama3.2

# Comma-separated CORS origins; use * to allow all
ALLOW_ORIGINS=*

# HTTP port for the service
PORT=3200
