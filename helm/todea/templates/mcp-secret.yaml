{{- if and .Values.mcp.enabled .Values.mcp.googleApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "todea.mcp.secretName" . }}
  labels:
    {{- include "todea.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp
type: Opaque
stringData:
  google-api-key: {{ .Values.mcp.googleApiKey | quote }}
{{- end }}
