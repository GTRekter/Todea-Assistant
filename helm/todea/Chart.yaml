apiVersion: v2
name: todea
description: "Helm chart for deploying the Todea-Assistant platform: React web UI, Agent Hub (Gemini), MCP server, and optional Ollama Hub + in-cluster Ollama runtime."
type: application
version: 0.4.0
appVersion: "1.0.0"
